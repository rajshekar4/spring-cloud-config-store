spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rajshekar4/spring-cloud-config-store
          search-paths:
           - '*service'
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: Qwerty@123
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true

